struct SCdxHeader
{
	// DLL definitions
	dll decode_cdx.dll
	func cdx_decode_keyExpression alias keyfor

	u32			root;
	u32			free;
				u8[4];			// Not used
	u16			keyLength;
	u8			options;
	u8			signature;
				u8[486];		// Not used

	// Flags
	u16			order;
				u8[2];			// Not used
	u16			forClauseLength;
				u8[2];			// Not used
	
	// Key expression and FOR clause
	keyfor		s8 "Key and FOR Clause:"[514];
};
